const lista = [1,6,7,8,2];
let total = 0

function somaElementos (acumulado ,algarismo) {
   acumulado = algarismo , 0
}

const soma = lista.reduce((atual, acum) => atual + acum, 0);
console.log(soma)